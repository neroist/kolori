- (linux) libc++ required to build 
- (windows) SDL3.dll needs to be next to executable
- (windows) small freeze-up when taking a screenshot
- (windows) screen flashes black and white for a period when turning fullscreen
- (feature) zoom in towards mouse cursur current position
- (feature) "p" to pause animation, some key to reset animation
- (feature) gamma, digamma, beta, zeta functions; sum and product functions
- (feature) arbitrary precision rendering
- ldd (Linux):
        linux-vdso.so.1
        libc++.so.1
        libc++abi.so.1
        libSDL3.so.0
        libm.so.6
        libc.so.6
        libgcc_s.so.1
        /lib64/ld-linux-x86-64.so.2
- readelf (Linux):
        libc++.so.1
        libc++abi.so.1
        libSDL3.so.0
        libm.so.6
        libc.so.6
- dumpbin (Windows):
        KERNEL32.dll
        bcrypt.dll
        ADVAPI32.dll
        SDL3.dll
        USER32.dll
        SHELL32.dll
        IMM32.dll